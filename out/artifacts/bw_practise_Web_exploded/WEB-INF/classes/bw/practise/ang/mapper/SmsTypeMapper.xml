<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bw.practise.ang.mapper.SmsTypeMapper">
   <select id="findbyId" resultType="smsType">
         select * from tb_sms_type where id = #{id}
   </select>
   
   <select id="findAll" resultType="smsType">
   		 select * from tb_sms_type 
   </select>

</mapper>
